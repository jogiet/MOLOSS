# colors
true: bin_annot, color(always)

# use menhir
true: use_menhir

# dependencies
<src/smtminisat.ml>: package(minisat)
<src/smtmsat.ml>: package(msat)
<**/moloss.*>: package(unix), package(minisat), package(msat)

# compiler options
true: ccopt(-static)

# Local Variables:
# mode: conf
# End: